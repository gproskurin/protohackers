%
% vi:ft=erlang
%

{deps, [
]}.

{shell, [
    %{config, "config/sys.config"},
    {apps, [ph]}
]}.

{erl_opts, [debug_info]}.

{ct_opts, [
    {dir, "test/ct/src"}
]}.

{eunit_tests, []}.

{eunit_opts, [
    {dir, "src"}
]}.

